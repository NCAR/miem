#include "miem/util/error.hpp"

// Error types are header-only; this file ensures the library has at least
// one translation unit in util/ and provides a place for future error
// utilities (e.g., error message formatting).
